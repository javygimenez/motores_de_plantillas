<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Lista de productos</title>
</head>

<body>
    <h2 class="text-primary">Lista de productos:</h2>
    <br>
    <% if (productos.length===0) { %>
        <div class="col text-center bg-danger text-light">
            <h2>No se han agregado productos a la lista</h2>
        </div>
        <% } else { %>
            <table class="table table-dark table-striped container-md mt-5">
                <thead>
                    <tr class="row-cols-2">
                        <th scope="col">Producto</th>
                        <th scope="col">Precio</th>
                        <th scope="col">Imagen</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let i=0; i < productos.length; i++) { %>
                        <tr>                            
                            <td class="align-middle">
                                <%=productos[i].nombre%>
                            </td>
                            <td class="align-middle">
                                <%=productos[i].precio%>
                            </td>
                            <td class="align-middle"><img src="<%=productos[i].imagen%>" alt="<%=productos[i].nombre%>"></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        <% } %>
        <br>
        <div class="col text-center">
            <a href="./">
                <button class="btn btn-success">Agregar productos</button>
            </a>
        </div>
</body>

</html>